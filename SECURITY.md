# Security

Report vulnerabilities through GitHub private vulnerability reporting. CSV paths are untrusted and must remain within the chosen source. Copy export stages a new destination, refuses collisions and existing targets, skips symbolic links, and never moves or deletes source covers.
